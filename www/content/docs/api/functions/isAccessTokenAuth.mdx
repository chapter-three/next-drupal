[next-drupal](../globals.mdx) / isAccessTokenAuth

# Function: isAccessTokenAuth()

> **isAccessTokenAuth**(`auth`): `auth is AccessToken`

Defined in: [packages/next-drupal/src/next-drupal-base.ts:610](https://github.com/chapter-three/next-drupal/blob/99d53e7de9f279983acefda33ce5bc4643bafb3c/packages/next-drupal/src/next-drupal-base.ts#L610)

Checks if the provided auth configuration is access token auth.

## Parameters

### auth

[`NextDrupalAuth`](../type-aliases/NextDrupalAuth.mdx)

The auth configuration.

## Returns

`auth is AccessToken`

True if the auth configuration is access token auth, false otherwise.
